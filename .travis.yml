---
sudo: required
language: python
python:
  - "3.4"
services:
  - docker
before_install:
  - sudo apt-get update
  - sudo -H python -m pip install ansible docker molecule testinfra
  - sudo python -m pip install six==1.12.0
script:
  - molecule test
